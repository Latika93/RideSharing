package com.RideSharing.RideSharing.entity;

/**
 * Enum representing who cancelled the trip
 */
public enum CancelledBy {
    RIDER,   // Trip was cancelled by the rider
    DRIVER   // Trip was cancelled by the driver
}
